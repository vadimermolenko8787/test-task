version: '3.8'

services:
  php:
    build: .
    volumes:
      - .:/app
    working_dir: /app

  nginx:
    image: nginx:alpine
    ports:
      - "8000:80"
    volumes:
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf
      - .:/app
    depends_on:
      - php
